# 权益计算引擎

`lib/benefit-engine` 只包含纯函数，不依赖页面、数据库或 AI：

- `types.ts`：信用卡、结构化规则、场景、使用记录和结果类型。
- `schemas.ts`：服务端/表单 Zod 校验。
- `eligibility.ts`：规范化商户、参与/排除门店、门槛、限额、次数、日期、首尔星期/时段、渠道、地区、支付方式、报名、优惠券与预约资格。
- `calculator.ts`：现金、积分与里程的确定性计算，单笔/日/月/年截断、整数舍入与剩余额度。
- `ranking.ts`：先按信用卡选出最优权益，再按可用性和可量化价值排序，避免同卡重复占位。
- `reset.ts`：不依赖服务器本地时区的 Asia/Seoul 日/月/年边界与下次重置日。
- `explanations.ts`：将计算结果转为可解释的中文说明。

## 计算顺序

1. 对输入场景与已记录用量进行资格判断。
2. 使用整数运算计算理论价值；百分比结果向下取整到最小货币单位。
3. 应用单笔和每日上限。
4. 应用月度与年度剩余额度，取非负最小值；积分与里程保持各自单位，不伪装成现金价值。
5. 返回剩余金额、剩余次数、重置日期、资格原因和不确定性提示。
6. 推荐层先对每张卡选择最优权益，再比较不同卡片。

没有设置上限时统一返回 `null`，UI 显示“无限额”，不会把它误认为 0。AI 适配器只能提取候选规则；金额计算始终由该引擎完成。

目录权益在管理员确认前带有 `reviewRequired=true` 或 `confidence=needs_review`，资格判断会返回“此项优惠尚未完成结构化确认”，因此不会进入确定性推荐。官方规则的新版本会新建用户权益记录；过去消费保存的 `rule_snapshot` 永不被同步覆盖。
